# Slide source

`build_deck.mjs` generates the editable 13-slide PowerPoint draft in `../output/`.

The helper functions at the top define the shared typography, section header, footer, boxes, arrows, and notes. Edit slide content in the numbered blocks below them, then rebuild with the bundled presentation runtime.
