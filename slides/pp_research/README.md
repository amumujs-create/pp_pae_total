# PP Research Detailed deck

**정식(단일) PPTX:** [`../../output/PP_Research_Detailed_v2.pptx`](../../output/PP_Research_Detailed_v2.pptx)

- 빌드 소스: 이 폴더의 `build_pp_research_deck_v2.py` · `generate_figures.py`
- 로컬 작업 복사본도 `ppt/pp/`에 두지 않음 — 출력은 위 `output/`만
- 연구 레포(`pp-extrapolation`)에는 PPTX를 두지 않음 → 이 레포를 참고

Canonical naming: **PP-X** = paper-main top-level framework (`PPX_NAMING.md`)

- **SAAR** = historical alias / core architecture (`SAAR_NAMING.md`)
- **CCMR v2.2** = trajectory-domain executor only
- **CRT / GCIE** = rejected; Algorithm 1 미포함
- **DS03** = route-selection PASS, predictive-superiority FAIL
  (frozen PP-X fallback R² 0.8818 < Engression 0.9013)
- prior가 거절되면 fallback 또는 abstention
- **PAE** = future program; paper main이 아님

기존 파일명은 참조 안정성을 위해 유지한다. 과거 결과명과 artifact label은 생성 당시의 명칭을 보존한다.

Study index: `STUDY_APPENDIX.md`
